
// AWS Batch configuration

wave.enabled = true

aws {
    region = 'us-west-2'
    batch {
        cliPath = '/path/to/aws'
        platformType = 'fargate'
        jobRole = 'JOB ROLE ARN'
        executionRole = 'EXECUTION ROLE ARN'
    }
}
